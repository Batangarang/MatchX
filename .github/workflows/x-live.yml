name: Poll X

on:
  workflow_dispatch:

concurrency:
  group: poll-x
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - run: npm install
      - run: node x-posts.js
        env:
          GETXAPI_KEY: ${{ secrets.GETXAPI_KEY }}
      - run: node nwcfl-news.js
      - run: node x-insights.js
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          for f in x-posts.json x-posts-lastpoll.json x-insights.json x-insights-lastrun.json user-ids.json nwcfl-news.json nwcfl-news-seen.json cost-log.json; do
            [ -f "$f" ] && git add "$f"
          done
          git diff --staged --quiet || git commit -m "..."
          for i in 1 2 3; do
            if git pull --rebase --autostash origin main; then
              break
            fi
            git diff --name-only --diff-filter=U | while read -r f; do
              git checkout --theirs "$f" 2>/dev/null && git add "$f"
            done
            git rebase --continue --no-edit || git rebase --abort
          done
          git push
