name: Division round-up (post-match)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  roundup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - run: npm install
      - run: node division-posts.js
        env:
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
      - run: node division-insight.js
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A -- division-posts.json division-insights.json division-roundup-lastrun.json
          git diff --staged --quiet || git commit -m "Post-match division round-up"
          git push